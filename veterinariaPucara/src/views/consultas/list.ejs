<%- include('../layouts/layout', { body: `
<div class="d-flex align-items-center justify-content-between mb-3">
  <h2>Consultas de ${pet.name_pet}</h2>
  <a class="btn" href="/consultas/${pet.id}/nueva">Nueva consulta</a>
</div>
<div class="card shadow-sm">
  <div class="card-body p-0">
    <table class="tbl">
      <thead>
        <tr>
          <th>Fecha</th>
          <th>Motivo</th>
          <th>Diagnóstico</th>
          <th>Tratamiento</th>
        </tr>
      </thead>
      <tbody>
        ${consultas.map(c => `
        <tr>
          <td>${new Date(c.fecha).toLocaleString('es-CL')}</td>
          <td>${c.motivo}</td>
          <td>${c.diagnostico || '-'}</td>
          <td>${c.tratamiento || '-'}</td>
        </tr>
        `).join('')}
      </tbody>
    </table>
  </div>
</div>

` }) %>
